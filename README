To compile, type 'make'
To run, type 'prog'


parser.y is for bison
scanner.lex is for flex

right now the program just validates input, it does not give valuable output
right now the program takes input from stdio
program can be run with an input file with the following command:
	'prog < inputfile'
